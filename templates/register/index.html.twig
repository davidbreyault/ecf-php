{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}

    <section>
        <div class="container">
            <div class="profile-header unique-item">
                <a href="{{ path('home') }}" class="cta">Retour</a>
            </div>
            <h1>S'inscrire</h1>
            {{ form_start(register_form, { 'attr': {'class': 'form register-form'} }) }}
            {{ form_end(register_form) }}
        </div>
    </section>
    
{% endblock %}
